// ignore_for_file: constant_identifier_names

enum RequestMethod { METHOD_GET, METHOD_POST, METHOD_PUT }

enum ContentType { CONTENT_JSON, CONTENT_FORMDATA, CONTENT_MULTIPART }

enum ResponseCode { SUCCESS, ERROR, CANCEL, FAIL }

enum ResponseType { RES_TYPE_JSON }
